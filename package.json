{
  "name": "bytestart",
  "version": "1.0.0",
  "description": "ByteStart - Educational Platform",
  "main": "backend/src/server.js",
  "type": "module",
  "scripts": {
    "dev": "cd backend && npm run dev",
    "start": "npm run build && cd backend && npm start",
    "build": "cd frontend && npm run build",
    "seed": "cd backend && npm run seed"
  },
  "engines": {
    "node": "18.x"
  }
}
